services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./db:/app/db
      - ./public/uploads:/app/public/uploads
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./db/custom.db
    restart: unless-stopped

volumes:
  db-data:
